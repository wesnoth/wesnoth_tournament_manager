-- Migrate wiki_articles from separate language rows to JSON model
-- Aligns with FAQ/News pattern in the project
-- NOTE: Wiki table was already migrated to JSON format in previous operations
-- This migration is now a NO-OP (no operations needed) since target structure already exists
-- Migration file kept for audit trail purposes

-- No operations needed - wiki_articles already has JSON translations structure
